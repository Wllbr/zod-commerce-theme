// Compatibility entry retained for existing theme asset references.
// ProductPage.initStickyPurchase owns the single native purchase control.
// It docks only after its original position leaves the viewport.
