{% if products|length %}<section data-testid="store-home-products-slider" class="zod-section"><div class="container"><salla-products-slider data-testid="store-home-products-slider-widget" source="{{ products.source }}" limit="{{ limit }}" autoplay source-value="{{ products.source_value|json_encode }}" display-all-url="{{ display_all_url }}" block-title="{{ title }}" sub-title="{{ sub_title|raw }}" slider-id="zod-native-slider-{{ position }}" product-card-component="custom-salla-product-card"></salla-products-slider></div></section>{% endif %}
