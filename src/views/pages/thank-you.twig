{% extends "layouts.master" %}
{% block content %}<section class="zod-status-page"><div class="container"><div class="zod-status-card"><span class="zod-status-icon is-success"><i class="sicon-check"></i></span><h1>{{ page.title }}</h1><div class="zod-rich-text">{{ page.content|raw }}</div>{% if order %}<salla-order-details order-id="{{ order.id }}"></salla-order-details>{% endif %}<a href="{{ link('/') }}" class="zod-btn zod-btn--primary">{{ trans('common.elements.back_home') }}</a></div></div></section>{% endblock %}
