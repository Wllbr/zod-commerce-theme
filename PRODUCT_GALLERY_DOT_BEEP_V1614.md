# v1.6.14 — Gallery dot selection beep

The active gallery dot is now a static red dot with a brief one-shot ring pulse when a different image becomes selected. The old continuous glow has been removed.
